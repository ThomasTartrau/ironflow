target
.DS_Store
.cargo/*
!.cargo/audit.toml
!.cargo/config.toml
.mcp.json

# Dashboard build artifacts (built in CI before cargo build)
ironflow-api/dashboard/
.codegraph
.codegraph.cache
.envrc

.claude/worktrees

# mdBook build output (root only, not docs/book/)
/book/
